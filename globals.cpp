#include "globals.h"

QStringList registeredUsers; // Define the global variable
QString currentUser;         // Define the global variable
QMap<QPair<int, int>, int> zombieImpactCount; // Definición única
